# Copy to .env.local and fill in. .env.local is gitignored.

# Local dev Postgres (matches docker-compose.yml). In production (Vercel +
# Supabase) this is the pooled/pgbouncer connection string, port 6543,
# with ?pgbouncer=true appended.
DATABASE_URL="postgresql://charaka:charaka@localhost:5432/charaka?schema=public"

# Only needed in production. Supabase's direct (non-pooled) connection, port
# 5432 — Prisma uses this for migrations only, since pgbouncer in transaction
# mode can't run them. Leave unset locally (falls back to DATABASE_URL).
DIRECT_URL=""

# Server-side only — never exposed to the browser. OpenRouter key for the M3
# practice loop (model: nvidia/nemotron-3.5-lightning:free).
OPENROUTER_KEY=""

# Signs the sign-in session cookie (M4). Any long random string.
AUTH_SECRET=""

# Mailtrap API token for login-code email delivery (Mailtrap's production
# Sending API, not their sandbox/testing inbox — sandbox only delivers to
# Mailtrap's own test inbox, not a real recipient). Unset locally — the code
# is logged to the console and returned in the API response instead. Required
# in any deployed environment (Vercel prod/preview), or sign-in has no way to
# reach the user.
MAILTRAP_API_TOKEN=""

# The "From" address login-code emails are sent as. Must be an address on a
# domain you've verified in Mailtrap (Sending → Domains) — required whenever
# MAILTRAP_API_TOKEN is set, production sending fails without a verified
# domain.
EMAIL_FROM=""

# Public site origin. Set this to your deployed URL (e.g.
# https://your-app.vercel.app) in Vercel's env vars.
NEXT_PUBLIC_SITE_URL=""
