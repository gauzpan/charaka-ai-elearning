# Copy to .env.local and fill in. .env.local is gitignored.

# Local dev Postgres (matches docker-compose.yml). In production (Vercel +
# Supabase) this is the pooled/pgbouncer connection string, port 6543,
# with ?pgbouncer=true appended.
DATABASE_URL="postgresql://charaka:charaka@localhost:5432/charaka?schema=public"

# Only needed in production. Supabase's direct (non-pooled) connection, port
# 5432 — Prisma uses this for migrations only, since pgbouncer in transaction
# mode can't run them. Leave unset locally (falls back to DATABASE_URL).
DIRECT_URL=""

# Server-side only — never exposed to the browser. OpenRouter key for the M3
# practice loop (model: nvidia/nemotron-3.5-lightning:free).
OPENROUTER_KEY=""

# Signs the magic-link session cookie (M4). Any long random string.
AUTH_SECRET=""

# Resend API key for magic-link email delivery. Unset locally — the link is
# logged to the console and returned in the API response instead. Required in
# any deployed environment (Vercel prod/preview), or sign-in has no way to
# reach the user.
RESEND_API_KEY=""

# The "From" address magic-link emails are sent as. Must be either Resend's
# shared testing address (onboarding@resend.dev — can only deliver to the
# email on your own Resend account) or an address on a domain you've verified
# in Resend. Defaults to the testing address if unset.
EMAIL_FROM=""

# Public site origin used to build the magic-link URL. Set this to your
# deployed URL (e.g. https://your-app.vercel.app) in Vercel's env vars —
# without it, links are built from the request origin, which is unreliable
# behind Vercel's proxy.
NEXT_PUBLIC_SITE_URL=""
